# 실시간 동기화(전국 SOS 카운트 · 공유 피드) 백엔드.
# 비워두면 앱은 백엔드 없이 로컬 상태로만 동작한다.
#
# Supabase 대시보드 → Project Settings → Data API 에서 가져온다.
# anon 키는 브라우저 번들에 그대로 실리는 공개 키다. service_role 키는 절대 넣지 말 것.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
